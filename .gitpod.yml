image:
  file: .gitpod.Dockerfile


tasks:
  - command: |
      deno run --allow-net --unstable webserver.ts

ports:
  - port: 8080
    onOpen: open-preview

vscode:
  extensions:
    - "denoland.vscode-deno"